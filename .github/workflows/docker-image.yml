# Replaced by .github/workflows/ci.yml and .github/workflows/deploy.yml
# This workflow is disabled — do not re-enable.
name: Docker Image CI (disabled)

on:
  workflow_dispatch: # manual only — never runs automatically

jobs:
  disabled:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - run: echo "This workflow has been replaced by ci.yml and deploy.yml"
