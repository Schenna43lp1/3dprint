services:
  web:
    build: .
    container_name: 3dprint_web
    restart: unless-stopped
    ports:
      - "8080:80"
    # Lokales Dev: Live-Code-Reload der Website.
    # Im Deployment wird stattdessen der ins Image kopierte Code genutzt.
    volumes:
      - ./3ddruck-suedtirol:/var/www/html
